apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-bootstrap
  namespace: argocd
  annotations:
    note: "ArgoCD installed via: kubectl apply -n argocd -f https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml"
    version: "stable"
